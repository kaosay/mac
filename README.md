# mac
how to use mac

## How to install brew on mac

/bin/zsh -c "$(curl -fsSL https://gitee.com/cunkai/HomebrewCN/raw/master/Homebrew.sh)"

#### install colima and docker on mac
```
brew install --formula docker
brew install colima
colima start
```
